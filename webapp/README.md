# manual

npm install
npm install -g @aws-amplify/cli

// powershell
amplify configure
// configure aws credentials

amplify init // (once per environment necessary)
amplify push

http://localhost:8080
